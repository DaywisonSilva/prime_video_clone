import styled from 'styled-components/native';

const StyledLoading = styled.SafeAreaView`
  background-color: ${props => props.theme.backgroundColor};
  flex: 1;
  justify-content: center;
`;

export {StyledLoading};
